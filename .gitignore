# Secrets / environment — never commit these
.env
*.env
credentials*

# Python virtual environments
.venv/
venv/

# Python caches
__pycache__/
*.pyc
.pytest_cache/

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# SEC staging output — reviewed, never committed
data/sec_staging/

# Power BI project local artifacts (per-machine cache/settings)
reports/ML Tool.SemanticModel/.pbi/cache.abf
reports/ML Tool.SemanticModel/.pbi/localSettings.json
reports/ML Tool.Report/.pbi/localSettings.json

# Office lock files (created while a workbook/doc is open)
~$*
**/~$*

# Live equity fetches - staged for review, never committed
data/market_staging/
